import { ChantierEtape } from "@/components/ChantierEtape";

export default function CoursPage() {
  return (
    <ChantierEtape
      titre="Cours"
      etape="Étape 3 (UI de base)"
      description="Lecture des cours par galop puis par domaine, avec schémas SVG intégrés. Le contenu du Galop 1 arrive à l'étape 2."
    />
  );
}
