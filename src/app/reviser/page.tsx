import { ChantierEtape } from "@/components/ChantierEtape";

export default function ReviserPage() {
  return (
    <ChantierEtape
      titre="Réviser"
      etape="Étape 3-4"
      description="Fiches de révision (format mémo, points-clés) et mode révision intelligente (répétition espacée : les questions ratées reviennent plus souvent)."
    />
  );
}
